export interface CreateIncomeAccountFormValues {
  account_number: string;
  name: string;
  regional_center_id: string;
  branch_id: string;
}

export interface EditIncomeAccountFormValues {
  account_number: string;
  name: string;
  regional_center_id: string;
  branch_id: string;
}
